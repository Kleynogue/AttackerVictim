const SosModel = () => {

    const handleAlertSQL = () => {
        console.log('Este metodo debe enviar la alerta del SOS'); 
    };

    return {
        handleAlertSQL,
    };
};

export default SosModel;