const CreateUserModel = () => {

    const handleCreateSQL = () => {
        console.log('Este metodo debe realizar la peticion SQL para crear un usuario'); 
    };

    return {
        handleCreateSQL,
    };
};

export default CreateUserModel;