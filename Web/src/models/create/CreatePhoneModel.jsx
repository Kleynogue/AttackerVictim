const CreatePhoneModel = () => {

    const handleCreateSQL = () => {
        console.log('Este metodo debe realizar la peticion SQL para crear un telefono'); 
    };

    return {
        handleCreateSQL,
    };
};

export default CreatePhoneModel;