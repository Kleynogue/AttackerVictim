const CreateCaseModel = () => {

    const handleCreateSQL = () => {
        console.log('Este metodo debe realizar la peticion SQL para crear un caso'); 
    };

    return {
        handleCreateSQL,
    };
};

export default CreateCaseModel;