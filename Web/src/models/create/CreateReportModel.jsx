const CreateReportModel = () => {

    const handleCreateSQL = () => {
        console.log('Este metodo debe realizar la peticion SQL para crear un reporte'); 
    };

    return {
        handleCreateSQL,
    };
};

export default CreateReportModel;